<nav class="bg-white border-b border-gray-200">
  <div class="container mx-auto px-6 py-4 flex justify-between items-center">
    <div class="text-2xl font-semibold text-gray-800">
      <%= link_to "PassPort", root_path %>
    </div>
    <ul class="flex space-x-6 items-center">
      <li>
        <%= link_to "Home", root_path, class: "text-gray-700 hover:text-blue-600 transition duration-200" %>
      </li>
      <li>
        <%= link_to "Events", events_path, class: "text-gray-700 hover:text-blue-600 transition duration-200" %>
      </li>
      <li>
        <a href="/tickets" class="text-gray-700 hover:text-blue-600 transition duration-200">Tickets</a>
      </li>
      
      <% if authenticated? %>
        <li>
          <%= link_to "Sign Out", session_path, method: :delete, class: "text-red-600 hover:text-red-800 transition duration-200" %>
        </li>
      <% else %>
        <li>
          <%= link_to "Sign In", new_session_path, class: "text-gray-700 hover:text-blue-600 transition duration-200" %>
        </li>
        <li>
          <%= link_to "Sign Up", new_registration_path, class: "bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition duration-200" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>